'use strict';

const FindMaxValue  = require('../FindMaxValue/index');

describe('Testing to find the max value of a binary tree', () => {
  const tree = new Tree(6);
  tree.insert(3);
  tree.insert(5);
  tree.insert(7);
  tree.insert(15)
)


